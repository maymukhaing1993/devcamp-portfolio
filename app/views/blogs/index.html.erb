<p style="color: green"><%= notice %></p>

<h1>Blogs</h1>

<div id="blogs">
  <table>
    <thead>
      <tr>
        <th>Title</th>
        <th>Body</th>
      </tr>
    </thead>
    <tbody>
  <% @blogs.each do |blog| %>
    <tr>
          <td><%= blog.title %></td>
          <td><%= blog.body %></td>
      <td><%= link_to "Show", blog %></td>
      <td><%= link_to "Edit", edit_blog_path(blog) %></td>
      <td><%= link_to "Destory", blog,method: :delete %></td>
    </tr>
  <% end %>
  </tbody>
  </table>
</div>

<%= link_to "New blog", new_blog_path %>
